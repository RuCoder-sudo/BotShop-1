# SHOP-Q - Telegram Маркетплейс Виртуальных Товаров

🚀 **Автоматизированный Telegram-бот для создания маркетплейса виртуальных товаров с географической привязкой**

SHOP-Q предоставляет полнофункциональную платформу для продавцов и покупателей с многоуровневой системой ролей, географической иерархией и встроенными инструментами управления.

---

## 📋 Содержание

- [Описание проекта](#описание-проекта)
- [Основные возможности](#основные-возможности)
- [Структура проекта](#структура-проекта)
- [Установка и настройка](#установка-и-настройка)
- [Запуск бота](#запуск-бота)
- [Автозапуск на сервере](#автозапуск-на-сервере)
- [Полезные команды для управления](#полезные-команды-для-управления)
- [Использование бота](#использование-бота)
- [База данных](#база-данных)
- [Конфигурация](#конфигурация)
- [Безопасность](#безопасность)

---

## 🎯 Описание проекта

SHOP-Q - это автоматизированный Telegram бот для создания маркетплейса виртуальных товаров. Бот предоставляет возможность множественным продавцам размещать свои товары, привязанные к конкретным географическим локациям (страна → город → район), а покупателям — удобно находить и покупать товары в своем регионе.

### ✨ Ключевые особенности:

- ✅ Многоуровневая система ролей: **Super Admin / Manager (Seller) / Buyer**
- ✅ Географическая иерархия для каждого товара (страна → город → район)
- ✅ Привязка товаров к локации
- ✅ Модульная структура (управление категориями, акции, реферальная система, техподдержка)
- ✅ Гибкие настройки в `config.py`
- ✅ Система верификации продавцов
- ✅ Корзина покупок и избранное
- ✅ Рейтинговая система (1.1 - 5.5 звезд)

---

## 🔥 Основные возможности

### Для Super Admin (Главный Администратор):

- **Полный контроль над системой**
  - Управление пользователями (блокировка, изменение ролей, балансов)
  - Верификация продавцов
  - Управление локациями (страны, города, районы)
  - Управление категориями товаров
  - Рассылка сообщений всем пользователям
  - Просмотр статистики системы
  
- **Инструменты администратора**
  - Управление промо-кодами
  - Обработка жалоб
  - Экспорт данных
  - Просмотр логов системы

- **Настройки бота**
  - Реферальная программа
  - Платежные системы (Bitcoin, Ethereum, карта, наличные)
  - Анти-флуд
  - Рейтинговая система

### Для Manager (Продавец):

- **Управление товарами**
  - Создание товаров с описанием, ценой, фото
  - Привязка к локации (страна-город-район)
  - Категоризация товаров
  - Управление остатками

- **Продажи и финансы**
  - Просмотр истории продаж
  - Статистика по товарам
  - Управление балансом
  - Рейтинг продавца

- **Информация и поддержка**
  - Справка для продавцов
  - Техническая поддержка

### Для Buyer (Покупатель):

- **Навигация и покупки**
  - Выбор локации (страна → город → район)
  - Просмотр магазинов продавцов в районе
  - Каталог товаров с фильтрами
  - Корзина покупок
  - Избранное

- **Поиск и фильтрация**
  - Поиск товаров по названию
  - Фильтр по локации
  - Просмотр рейтингов

- **Личный кабинет**
  - История заказов
  - Просмотр профиля и баланса
  - Реферальная программа
  - Поддержка

---

## 📁 Структура проекта

```
SHOP-Q/
│
├── main.py                 # Главная точка входа, запуск бота
├── config.py              # Конфигурация: токены, ID админов, настройки
├── requirements.txt       # Python зависимости
│
├── database/              # Модуль работы с базой данных
│   ├── __init__.py
│   └── database.py        # Класс Database со всеми методами работы с БД
│
├── handlers/              # Обработчики команд и сообщений
│   ├── __init__.py
│   ├── start.py          # Обработка /start, выбор роли, меню
│   ├── admin.py          # Функции для администратора
│   ├── manager.py        # Функции для продавцов
│   └── buyer.py          # Функции для покупателей
│
├── utils/                 # Вспомогательные функции
│   ├── __init__.py
│   └── helpers.py        # Генерация кодов, форматирование, проверки
│
├── images/               # Хранилище изображений
│   └── products/         # Фотографии товаров
│
├── shop_q.db             # База данных SQLite (создается автоматически)
├── shop_q.log            # Файл логов (создается автоматически)
│
├── README.md             # Документация проекта
└── TZ-SHOP.md            # Техническое задание и статус выполнения
```

### Описание ключевых файлов:

#### `main.py`
Главный файл приложения. Инициализирует бота, базу данных, регистрирует все обработчики команд и запускает polling.

**Основные функции:**
- `main()` - точка входа, создает Application и запускает бота
- `start_command()` - обработчик команды /start
- `role_selection_callback()` - обработка выбора роли пользователем

#### `config.py`
Файл конфигурации со всеми настройками бота.

**Основные параметры:**
- `BOT_TOKEN` - токен Telegram бота (из переменных окружения)
- `SUPER_ADMIN_IDS` - список Telegram ID администраторов
- `NOTIFICATION_CHANNEL_ID` - ID канала для уведомлений
- `PAYMENT_METHODS` - настройки платежных систем
- `REFERRAL_BONUS_*` - бонусы реферальной программы
- `ANTI_FLOOD_*` - настройки защиты от флуда

#### `database/database.py`
Класс для работы с SQLite базой данных. Содержит методы для:
- Управления пользователями
- Работы с локациями (страны, города, районы)
- Управления категориями
- Работы с товарами
- Обработки заказов
- Корзины и избранного
- Рейтингов и отзывов
- Верификации продавцов

#### `handlers/start.py`
Обработчики начального взаимодействия с ботом:
- Регистрация новых пользователей
- Выбор роли (покупатель/продавец)
- Отображение главного меню для каждой роли

#### `handlers/admin.py`
Все функции для администратора:
- Управление пользователями
- Верификация продавцов
- CRUD операции с локациями (создание, редактирование, удаление)
- CRUD операции с категориями
- Рассылки сообщений
- Статистика
- Инструменты и настройки

#### `handlers/manager.py`
Функционал для продавцов:
- Создание товаров (многошаговый процесс)
- Просмотр своих товаров
- История продаж
- Статистика
- Информация и поддержка

#### `handlers/buyer.py`
Функционал для покупателей:
- Выбор локации и просмотр магазинов
- Просмотр товаров
- Корзина покупок
- Избранное
- История заказов
- Профиль
- Поиск товаров

#### `utils/helpers.py`
Вспомогательные функции:
- `generate_referral_code()` - генерация реферального кода
- `generate_order_number()` - генерация номера заказа
- `check_admin()` - проверка прав администратора
- `check_manager()` - проверка роли продавца
- `format_price()` - форматирование цены
- `format_rating()` - форматирование рейтинга

---

## 🚀 Установка и настройка

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd shop-q
```

### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

**Зависимости:**
- `python-telegram-bot==20.8` - библиотека для работы с Telegram Bot API
- `python-dotenv==1.0.0` - загрузка переменных окружения из .env файла

### 3. Получение токена бота

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Получите токен в формате: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`

### 4. Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```env
BOT_TOKEN=Ваш токен бота 
NOTIFICATION_CHANNEL_ID=Ваш айди групып
SUPER_ADMIN_IDS=Ваш айди супер админа
```

**Или** установите переменные окружения напрямую:

```bash
export BOT_TOKEN="Ваш токен бота "
export NOTIFICATION_CHANNEL_ID="Ваш айди групып"
export SUPER_ADMIN_IDS="Ваш айди супер админа"
```

### 5. Настройка администраторов

Откройте `config.py` и укажите ваш Telegram ID в `SUPER_ADMIN_IDS`:

```python
SUPER_ADMIN_IDS = [Ваш айди супер админа]
```

**Текущий бот:** @AldanLiverisBot  
**ID админа:** Ваш айди супер админа  
**ID группы уведомлений:** Ваш айди групып

### 6. Настройка ID канала уведомлений

ID группы для уведомлений уже настроен: **Ваш айди групып**

Убедитесь, что бот добавлен в группу как администратор.

---

## 🎮 Запуск бота

### Локальный запуск

```bash
python main.py
```

Вы должны увидеть в консоли:

```
🚀 Запуск SHOP-Q бота...
✅ База данных инициализирована
✅ Все обработчики зарегистрированы
🤖 Бот запущен! Админы: [Ваш айди супер админа]
📢 Канал уведомлений: Ваш айди групып
```

### Проверка работы

1. Откройте Telegram
2. Найдите бота **@AldanLiverisBot**
3. Отправьте `/start`
4. Должно появиться приветственное сообщение с кнопками выбора роли

### Данные бота

**Бот:** @AldanLiverisBot  
**Токен:** Ваш токен бота   
**ID группы:** Ваш айди групып  
**ID админа:** Ваш айди супер админа

---

## 🖥️ Автозапуск на сервере

### Вариант 1: systemd (Linux)

Создайте файл сервиса `/etc/systemd/system/shop-q.service`:

```ini
[Unit]
Description=SHOP-Q Telegram Bot
After=network.target

[Service]
Type=simple
User=your_username
WorkingDirectory=/path/to/shop-q
Environment="BOT_TOKEN=Ваш токен бота "
Environment="NOTIFICATION_CHANNEL_ID=Ваш айди групып"
ExecStart=/usr/bin/python3 /path/to/shop-q/main.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

**Активация:**

```bash
sudo systemctl daemon-reload
sudo systemctl enable shop-q
sudo systemctl start shop-q
sudo systemctl status shop-q
```

**Управление:**

```bash
sudo systemctl stop shop-q      # Остановка
sudo systemctl restart shop-q   # Перезапуск
sudo journalctl -u shop-q -f    # Просмотр логов
```

### Вариант 2: Docker

Создайте `Dockerfile`:

```dockerfile
FROM python:3.10-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

ENV BOT_TOKEN=""
ENV NOTIFICATION_CHANNEL_ID=""

CMD ["python", "main.py"]
```

Создайте `docker-compose.yml`:

```yaml
version: '3.8'

services:
  shop-q-bot:
    build: .
    container_name: shop-q
    restart: unless-stopped
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
      - NOTIFICATION_CHANNEL_ID=${NOTIFICATION_CHANNEL_ID}
    volumes:
      - ./shop_q.db:/app/shop_q.db
      - ./shop_q.log:/app/shop_q.log
      - ./images:/app/images
```

**Запуск:**

```bash
docker-compose up -d
docker-compose logs -f  # Просмотр логов
```

### Вариант 3: Screen/tmux (простой способ)

```bash
# С использованием screen
screen -S shop-q
python main.py
# Нажмите Ctrl+A, затем D для отсоединения

# Вернуться к сессии
screen -r shop-q

# С использованием tmux
tmux new -s shop-q
python main.py
# Нажмите Ctrl+B, затем D для отсоединения

# Вернуться к сессии
tmux attach -t shop-q
```

### Вариант 4: Supervisor

Установка Supervisor:

```bash
sudo apt-get install supervisor
```

Создайте файл `/etc/supervisor/conf.d/shop-q.conf`:

```ini
[program:shop-q]
command=/usr/bin/python3 /path/to/shop-q/main.py
directory=/path/to/shop-q
user=your_username
autostart=true
autorestart=true
stderr_logfile=/var/log/shop-q.err.log
stdout_logfile=/var/log/shop-q.out.log
environment=BOT_TOKEN="Ваш токен бота ",NOTIFICATION_CHANNEL_ID="Ваш айди групып"
```

**Управление:**

```bash
sudo supervisorctl reread
sudo supervisorctl update
sudo supervisorctl start shop-q
sudo supervisorctl status shop-q
```

---

## 🔧 Обслуживание и управление ботом

### Перезапуск бота

**Если бот запущен через systemd:**
```bash
sudo systemctl restart shop-q
```

**Если бот запущен через Docker:**
```bash
docker-compose restart
```

**Если бот запущен через supervisor:**
```bash
sudo supervisorctl restart shop-q
```

**Если бот запущен в screen/tmux:**
```bash
# Присоединитесь к сессии
screen -r shop-q  # или tmux attach -t shop-q

# Остановите бота (Ctrl+C)
# Запустите снова
python main.py
```

### Очистка логов

**Просмотр размера логов:**
```bash
ls -lh shop_q.log
```

**Очистка файла логов:**
```bash
# Полная очистка
> shop_q.log

# Или удаление старых логов
rm shop_q.log
```

**Ротация логов (рекомендуется):**

Создайте файл `/etc/logrotate.d/shop-q`:
```
/path/to/shop-q/shop_q.log {
    daily
    missingok
    rotate 14
    compress
    delaycompress
    notifempty
    create 0640 your_username your_username
    postrotate
        systemctl reload shop-q > /dev/null 2>&1 || true
    endscript
}
```

### Очистка кеша базы данных

**Резервное копирование перед очисткой:**
```bash
# Создать бэкап
cp shop_q.db shop_q_backup_$(date +%Y%m%d_%H%M%S).db
```

**Оптимизация базы данных:**
```bash
# Запустите VACUUM для очистки и оптимизации
sqlite3 shop_q.db "VACUUM;"

# Проверка размера БД
ls -lh shop_q.db
```

**Очистка старых данных (опционально):**
```bash
# Подключитесь к БД
sqlite3 shop_q.db

# Удалите старые заказы (например, старше 90 дней)
DELETE FROM orders WHERE created_at < datetime('now', '-90 days');

# Удалите старые корзины неактивных пользователей
DELETE FROM cart WHERE added_at < datetime('now', '-30 days');

# Оптимизируйте после удаления
VACUUM;

# Выход
.quit
```

### Мониторинг работы бота

**Просмотр логов в реальном времени:**
```bash
tail -f shop_q.log
```

**Поиск ошибок в логах:**
```bash
grep -i error shop_q.log | tail -20
```

**Проверка процесса:**
```bash
ps aux | grep "python.*main.py"
```

**Проверка использования ресурсов:**
```bash
# CPU и память
top -p $(pgrep -f "python.*main.py")

# Размер базы данных
du -h shop_q.db
```

### Резервное копирование

**Автоматическое резервное копирование (cron):**

Добавьте в crontab (`crontab -e`):
```bash
# Ежедневный бэкап в 3 часа ночи
0 3 * * * cp /path/to/shop-q/shop_q.db /path/to/backups/shop_q_$(date +\%Y\%m\%d).db

# Удаление бэкапов старше 30 дней
0 4 * * * find /path/to/backups/ -name "shop_q_*.db" -mtime +30 -delete
```

### Обновление бота

```bash
# 1. Остановите бота
sudo systemctl stop shop-q  # или другой метод

# 2. Сделайте бэкап
cp shop_q.db shop_q_backup.db

# 3. Получите обновления
git pull origin main  # если используете git

# 4. Обновите зависимости
pip install -r requirements.txt --upgrade

# 5. Запустите бота
sudo systemctl start shop-q
```

### Диагностика проблем

**Бот не отвечает:**
1. Проверьте, запущен ли процесс: `ps aux | grep python`
2. Проверьте логи: `tail -50 shop_q.log`
3. Проверьте токен: убедитесь, что BOT_TOKEN установлен
4. Проверьте интернет-соединение

**Ошибки базы данных:**
```bash
# Проверка целостности БД
sqlite3 shop_q.db "PRAGMA integrity_check;"

# Если БД повреждена, восстановите из бэкапа
cp shop_q_backup.db shop_q.db
```

**Проблемы с памятью:**
```bash
# Очистите кеш системы
sync; echo 3 > /proc/sys/vm/drop_caches

# Перезапустите бота
sudo systemctl restart shop-q
```

---

## ⚡ Быстрые команды (самые важные)

### 🚀 Управление ботом (systemd)

```bash
# Запустить бота
sudo systemctl start shop-q

# Остановить бота
sudo systemctl stop shop-q

# Перезапустить бота
sudo systemctl restart shop-q

# Статус бота
sudo systemctl status shop-q

# Логи в реальном времени
sudo journalctl -u shop-q -f
```

### 👥 Работа с пользователями (SQLite)

```bash
# Показать ВСЕХ пользователей (список)
sqlite3 shop_q.db "SELECT user_id, username, first_name, role, balance FROM users;"

# Красивый вывод таблицей
sqlite3 shop_q.db <<EOF
.headers on
.mode column
SELECT user_id, username, first_name, role, balance FROM users;
EOF

# Поиск пользователя по ID
sqlite3 shop_q.db "SELECT * FROM users WHERE user_id = Ваш айди супер админа;"

# Поиск по имени
sqlite3 shop_q.db "SELECT * FROM users WHERE username LIKE '%admin%';"

# Удалить пользователя по ID
sqlite3 shop_q.db "DELETE FROM users WHERE user_id = 123456789;"

# Удалить нескольких пользователей
sqlite3 shop_q.db "DELETE FROM users WHERE user_id IN (123456, 789012);"

# Изменить роль пользователя на админа
sqlite3 shop_q.db "UPDATE users SET role = 'admin' WHERE user_id = Ваш айди супер админа;"

# Изменить баланс
sqlite3 shop_q.db "UPDATE users SET balance = 1000.0 WHERE user_id = Ваш айди супер админа;"
```

### 📦 Работа с товарами

```bash
# Показать все товары
sqlite3 shop_q.db "SELECT id, name, price, seller_id FROM products;"

# Удалить товар
sqlite3 shop_q.db "DELETE FROM products WHERE id = 5;"

# Показать товары продавца
sqlite3 shop_q.db "SELECT id, name, price FROM products WHERE seller_id = Ваш айди супер админа;"
```

### 📊 Статистика

```bash
# Быстрая статистика по базе
sqlite3 shop_q.db <<EOF
.headers on
.mode column
SELECT 'Пользователи' as Таблица, COUNT(*) as Количество FROM users
UNION ALL SELECT 'Товары', COUNT(*) FROM products
UNION ALL SELECT 'Заказы', COUNT(*) FROM orders;
EOF
```

### 🗄️ Резервное копирование

```bash
# Создать бэкап базы
cp shop_q.db "shop_q_backup_$(date +%Y%m%d_%H%M%S).db"

# Восстановить из бэкапа
cp shop_q_backup_20251125_120000.db shop_q.db
```

---

## 📋 Полезные команды для управления

### 🚀 Управление ботом

**Запустить бота:**
```bash
python main.py
```

**Временно остановить бота:**
```bash
# Найдите PID процесса
ps aux | grep "python.*main.py"

# Остановите процесс (замените PID на реальный номер)
kill <PID>

# Или используйте pkill (работает в Termux)
pkill -f "python.*main.py"
```

**Перезапустить бота на Replit:**
```bash
# Остановите текущий процесс
pkill -f "python.*main.py"

# Запустите снова
python main.py
```

**Запустить бота в фоне (через nohup):**
```bash
nohup python main.py > bot_output.log 2>&1 &

# Проверить, что бот запущен
ps aux | grep "python.*main.py"
```

**Проверить статус бота:**
```bash
# Проверить, запущен ли процесс
ps aux | grep "python.*main.py"

# Проверить последние логи
tail -20 shop_q.log
```

### 💾 Работа с базой данных

**Просмотр всех пользователей:**
```bash
sqlite3 shop_q.db "SELECT user_id, username, first_name, role FROM users;"
```

**Просмотр пользователей в виде таблицы:**
```bash
sqlite3 shop_q.db <<EOF
.headers on
.mode column
SELECT user_id, username, first_name, role, balance FROM users;
EOF
```

**Удалить конкретного пользователя:**
```bash
# Удалить пользователя по ID
sqlite3 shop_q.db "DELETE FROM users WHERE user_id = 456861556;"

# Удалить нескольких пользователей
sqlite3 shop_q.db "DELETE FROM users WHERE user_id IN (456861556, 1555915895);"
```

**Просмотр всех товаров:**
```bash
sqlite3 shop_q.db "SELECT id, name, price, seller_id FROM products;"
```

**Просмотр категорий:**
```bash
sqlite3 shop_q.db "SELECT id, name, parent_id FROM categories ORDER BY parent_id, id;"
```

**Просмотр заказов:**
```bash
sqlite3 shop_q.db "SELECT order_number, buyer_id, seller_id, total_price, status FROM orders ORDER BY created_at DESC LIMIT 10;"
```

**Статистика базы данных:**
```bash
sqlite3 shop_q.db <<EOF
.headers on
.mode column
SELECT 'Пользователи' as Таблица, COUNT(*) as Количество FROM users
UNION ALL
SELECT 'Товары', COUNT(*) FROM products
UNION ALL
SELECT 'Категории', COUNT(*) FROM categories
UNION ALL
SELECT 'Заказы', COUNT(*) FROM orders
UNION ALL
SELECT 'Страны', COUNT(*) FROM countries
UNION ALL
SELECT 'Города', COUNT(*) FROM cities
UNION ALL
SELECT 'Районы', COUNT(*) FROM districts;
EOF
```

**Изменить баланс пользователя:**
```bash
# Установить баланс
sqlite3 shop_q.db "UPDATE users SET balance = 1000.0 WHERE user_id = Ваш айди супер админа;"

# Добавить к балансу
sqlite3 shop_q.db "UPDATE users SET balance = balance + 500.0 WHERE user_id = Ваш айди супер админа;"
```

**Изменить роль пользователя:**
```bash
# Сделать администратором
sqlite3 shop_q.db "UPDATE users SET role = 'admin' WHERE user_id = Ваш айди супер админа;"

# Сделать продавцом
sqlite3 shop_q.db "UPDATE users SET role = 'manager' WHERE user_id = 6987778017;"

# Сделать покупателем
sqlite3 shop_q.db "UPDATE users SET role = 'buyer' WHERE user_id = 123456789;"
```

**Очистить корзину пользователя:**
```bash
sqlite3 shop_q.db "DELETE FROM cart WHERE user_id = 123456789;"
```

**Резервная копия базы данных:**
```bash
# Создать резервную копию
cp shop_q.db "shop_q_backup_$(date +%Y%m%d_%H%M%S).db"

# Восстановить из резервной копии
cp shop_q_backup_20251123_120000.db shop_q.db
```

**Экспорт данных в CSV:**
```bash
# Экспорт пользователей
sqlite3 shop_q.db <<EOF
.headers on
.mode csv
.output users_export.csv
SELECT * FROM users;
EOF

# Экспорт товаров
sqlite3 shop_q.db <<EOF
.headers on
.mode csv
.output products_export.csv
SELECT * FROM products;
EOF
```

**Оптимизация базы данных:**
```bash
# Очистить неиспользуемое пространство
sqlite3 shop_q.db "VACUUM;"

# Проверить целостность
sqlite3 shop_q.db "PRAGMA integrity_check;"
```

### 📄 Работа с логами

**Просмотр логов в реальном времени:**
```bash
tail -f shop_q.log
```

**Просмотр последних 50 строк логов:**
```bash
tail -50 shop_q.log
```

**Поиск ошибок в логах:**
```bash
grep -i error shop_q.log | tail -20
```

**Поиск предупреждений:**
```bash
grep -i warning shop_q.log | tail -20
```

**Поиск по конкретному пользователю:**
```bash
grep "user_id: Ваш айди супер админа" shop_q.log | tail -20
```

**Очистить файл логов:**
```bash
# Полная очистка (рекомендуется перед очисткой сделать копию)
> shop_q.log

# Или удалить файл логов
rm shop_q.log
```

**Архивировать логи:**
```bash
# Сжать и сохранить старые логи
gzip -c shop_q.log > "shop_q_logs_$(date +%Y%m%d).log.gz"

# Очистить текущий файл
> shop_q.log
```

**Просмотр логов systemd (если бот запущен через systemd):**
```bash
# Просмотр всех логов
journalctl -u shop-q

# Просмотр логов в реальном времени
journalctl -u shop-q -f

# Просмотр логов за сегодня
journalctl -u shop-q --since today

# Просмотр последних 100 строк
journalctl -u shop-q -n 100
```

**Размер файла логов:**
```bash
ls -lh shop_q.log
du -h shop_q.log
```

### 🗑️ Удаление и очистка проекта

**Полностью удалить проект:**
```bash
# ВНИМАНИЕ: Это удалит ВСЕ данные без возможности восстановления!

# 1. Остановите бота
pkill -f "python.*main.py"

# 2. Перейдите на уровень выше папки проекта
cd ..

# 3. Удалите всю папку проекта
rm -rf shop-q/

# Или, если находитесь внутри папки проекта:
cd ..
rm -rf "$(pwd)"
```

**Очистить только данные (сохранить код):**
```bash
# Удалить базу данных
rm shop_q.db

# Удалить логи
rm shop_q.log

# Удалить изображения товаров
rm -rf images/products/*

# Удалить резервные копии
rm -f shop_q_backup_*.db
```

**Сбросить базу данных (начать с чистого листа):**
```bash
# 1. Остановите бота
pkill -f "python.*main.py"

# 2. Удалите старую базу
rm shop_q.db

# 3. Запустите бота снова (база создастся автоматически)
python main.py
```

**Очистить кеш Python:**
```bash
# Удалить __pycache__ файлы
find . -type d -name "__pycache__" -exec rm -rf {} +
find . -type f -name "*.pyc" -delete
```

**Очистить временные файлы:**
```bash
# Удалить временные файлы
rm -f *.tmp
rm -f *.log.gz
rm -f nohup.out
rm -f bot_output.log
```

### 🔍 Мониторинг и диагностика

**Использование ресурсов:**
```bash
# CPU и память процесса бота
ps aux | grep "python.*main.py"

# Детальная информация о процессе
top -p $(pgrep -f "python.*main.py")
```

**Размеры файлов проекта:**
```bash
# Размер базы данных
du -h shop_q.db

# Размер всех изображений
du -sh images/

# Размер всего проекта
du -sh .
```

**Проверка доступности бота:**
```bash
# Проверить, отвечает ли бот через API
curl -s "https://api.telegram.org/bot${BOT_TOKEN}/getMe" | python -m json.tool
```

**Количество файлов:**
```bash
# Количество изображений товаров
ls -1 images/products/ | wc -l

# Количество Python файлов
find . -name "*.py" | wc -l
```

**Проверка переменных окружения:**
```bash
# Вывести все переменные
env | grep -E "BOT_TOKEN|NOTIFICATION_CHANNEL_ID|SUPER_ADMIN_IDS"

# Проверить, установлена ли переменная
echo $BOT_TOKEN
```

### 🔧 Быстрые команды для разработки

**Перезапуск после изменений в коде:**
```bash
pkill -f "python.*main.py" && python main.py
```

**Просмотр структуры проекта:**
```bash
tree -L 2 -I '__pycache__|*.pyc'
```

**Подсчет строк кода:**
```bash
find . -name "*.py" -exec wc -l {} + | tail -1
```

**Поиск TODO в коде:**
```bash
grep -rn "TODO" --include="*.py"
```

**Быстрая проверка синтаксиса Python:**
```bash
python -m py_compile main.py
```

### 📦 Миграция данных

**Экспорт всей базы в SQL:**
```bash
sqlite3 shop_q.db .dump > shop_q_dump.sql
```

**Импорт базы из SQL:**
```bash
sqlite3 new_shop_q.db < shop_q_dump.sql
```

**Копировать базу на другой сервер:**
```bash
# На текущем сервере
scp shop_q.db user@remote-server:/path/to/destination/

# Или через rsync
rsync -avz shop_q.db user@remote-server:/path/to/destination/
```

---

## 📖 Использование бота

### Для администратора:

1. **Запустите бота** - `/start`
2. **Автоматически получите админ-панель** (если ваш ID в SUPER_ADMIN_IDS)

**Доступные функции:**
- 👥 **Пользователи** - управление пользователями, просмотр списка
- ✅ **Верификация** - одобрение/отклонение заявок продавцов
- 🌍 **Локации** - добавление/редактирование/удаление стран, городов, районов
- 📁 **Категории** - управление категориями товаров
- 📊 **Статистика** - просмотр статистики бота
- 📣 **Рассылка** - отправка сообщений всем пользователям
- 🛠️ **Инструменты** - промо-коды, жалобы, экспорт данных
- ⚙️ **Настройки** - просмотр текущих настроек системы

### Для продавца:

1. **Запустите бота** - `/start`
2. **Выберите роль** - "💼 Я продавец"
3. **Дождитесь верификации** - администратор должен одобрить вашу заявку

**После верификации доступно:**
- ➕ **Создать товар** - пошаговое создание товара с фото
- 📦 **Мои товары** - список ваших товаров
- 🧾 **Продажи** - история продаж
- 📊 **Статистика** - ваша статистика как продавца
- ℹ️ **Информация** - справка для продавцов
- 💬 **Поддержка** - техподдержка

### Для покупателя:

1. **Запустите бота** - `/start`
2. **Выберите роль** - "🛍 Я покупатель"

**Доступно:**
- 🏪 **Магазины** - выбор локации и просмотр продавцов
- 🔍 **Поиск** - поиск товаров по названию
- 🛒 **Корзина** - ваша корзина покупок
- 📦 **Мои заказы** - история заказов
- 👤 **Профиль** - баланс и реферальная программа
- ⭐ **Избранное** - избранные товары
- ℹ️ **Информация** - справка о боте
- 💬 **Поддержка** - техподдержка

---

## 💾 База данных

Бот использует **SQLite** базу данных (`shop_q.db`).

### Структура таблиц:

1. **users** - Пользователи
   - user_id, username, first_name, phone
   - role (pending/buyer/manager/admin)
   - balance, blocked, verification_status
   - referrer_id, referral_code

2. **countries** - Страны
   - id, name, created_at, created_by

3. **cities** - Города
   - id, country_id, name, created_at, created_by

4. **districts** - Районы
   - id, city_id, name, created_at, created_by

5. **categories** - Категории товаров
   - id, name, parent_id (для подкатегорий)

6. **products** - Товары
   - id, seller_id, name, description, price, stock
   - category_id, country_id, city_id, district_id
   - image_path, status

7. **orders** - Заказы
   - id, order_number, buyer_id, seller_id, product_id
   - quantity, total_price, status, payment_method

8. **cart** - Корзина
   - user_id, product_id, quantity

9. **ratings** - Рейтинги и отзывы
   - order_id, buyer_id, seller_id, product_id
   - rating (1.1-5.5), comment

10. **favorites** - Избранное
    - user_id, product_id

11. **verification_requests** - Заявки на верификацию
    - user_id, status, requested_at, processed_by

12. **transactions** - Транзакции
    - user_id, amount, type, description

13. **promo_codes** - Промо-коды
    - code, discount_percent, usage_limit

14. **support_tickets** - Тикеты поддержки
    - user_id, subject, message, status

15. **complaints** - Жалобы
    - complainant_id, target_id, product_id, reason

### Резервное копирование:

```bash
# Создать копию базы данных
cp shop_q.db shop_q_backup_$(date +%Y%m%d).db

# Автоматическое резервное копирование (cron)
0 2 * * * cp /path/to/shop_q.db /path/to/backups/shop_q_$(date +\%Y\%m\%d).db
```

---

## ⚙️ Конфигурация

Все настройки в файле `config.py`:

### Основные настройки:

```python
SUPER_ADMIN_IDS = [456861556]  # Telegram ID администраторов
NOTIFICATION_CHANNEL_ID = -1003154951959  # ID канала уведомлений
DATABASE_NAME = "shop_q.db"  # Имя файла базы данных
```

### Платежные системы:

```python
PAYMENT_METHODS = {
    'bitcoin': {'enabled': True, 'wallet': '', 'name': 'Bitcoin (BTC)'},
    'ethereum': {'enabled': True, 'wallet': '', 'name': 'Ethereum (ETH)'},
    'card': {'enabled': True, 'name': 'Карта курьеру'},
    'cash': {'enabled': True, 'name': 'Наличные'}
}
```

### Реферальная программа:

```python
REFERRAL_BONUS_INVITER = 100  # Бонус приглашающему (руб)
REFERRAL_BONUS_INVITED = 50   # Бонус приглашенному (руб)
```

### Анти-флуд:

```python
ANTI_FLOOD_LIMIT = 5        # Лимит запросов
ANTI_FLOOD_TIMEOUT = 60     # Таймаут (секунды)
```

### Рейтинговая система:

```python
MIN_RATING = 1.1  # Минимальный рейтинг
MAX_RATING = 5.5  # Максимальный рейтинг
```

---

## 🔒 Безопасность

### Рекомендации:

1. **Никогда не коммитьте токен бота** в Git
   - Используйте `.env` файл
   - Добавьте `.env` в `.gitignore`

2. **Используйте переменные окружения**
   ```bash
   export BOT_TOKEN="ваш_токен"
   ```

3. **Ограничьте права доступа к файлам**
   ```bash
   chmod 600 .env
   chmod 600 shop_q.db
   ```

4. **Регулярно обновляйте зависимости**
   ```bash
   pip install --upgrade -r requirements.txt
   ```

5. **Делайте резервные копии базы данных**

6. **Используйте HTTPS для вебхуков** (если используете)

7. **Проверяйте логи на подозрительную активность**
   ```bash
   tail -f shop_q.log
   ```

---

## 📝 Логирование

Все события записываются в:
- **Консоль** - для мониторинга в реальном времени
- **shop_q.log** - файл с полной историей

Уровни логирования:
- `INFO` - обычные операции
- `ERROR` - ошибки и исключения

Просмотр логов:

```bash
# Последние 100 строк
tail -n 100 shop_q.log

# В реальном времени
tail -f shop_q.log

# Фильтр только ошибок
grep ERROR shop_q.log
```

---

## 🆘 Поддержка и FAQ

### 🔧 Установка и настройка

**Q: Бот не запускается, что делать?**  
A: Проверьте следующее:
1. Установлен ли токен бота: `echo $BOT_TOKEN`
2. Установлены ли зависимости: `pip install -r requirements.txt`
3. Нет ли конфликтующих пакетов (убедитесь, что установлен только `python-telegram-bot`, а не `telegram`)
4. Проверьте логи: `tail -50 shop_q.log`

**Q: Ошибка "BOT_TOKEN не установлен"**  
A: Установите переменную окружения:
```bash
export BOT_TOKEN="Ваш токен бота "
```
Или создайте файл `.env` с этим токеном.

**Q: Ошибка при установке зависимостей**  
A: Убедитесь, что используете Python 3.10 или выше:
```bash
python --version
pip install --upgrade pip
pip install -r requirements.txt
```

**Q: База данных не создается**  
A: Проверьте права доступа к директории:
```bash
ls -la shop_q.db
chmod 644 shop_q.db  # Если файл существует
```

---

### 👥 Роли и доступ

**Q: Как получить права администратора?**  
A: Добавьте свой Telegram ID в `config.py`:
```python
SUPER_ADMIN_IDS = [ваш_telegram_id]
```
Чтобы узнать свой ID, напишите боту `/start` и проверьте логи.

**Q: Как стать продавцом?**  
A: 
1. Отправьте `/start` боту
2. Выберите "💼 Я продавец"
3. Дождитесь одобрения администратором
4. После одобрения вы получите доступ к меню продавца

**Q: Не приходит меню продавца после верификации**  
A: После одобрения администратором:
1. Бот автоматически отправит вам меню продавца
2. Если не пришло, отправьте `/start` заново
3. Убедитесь, что ваша заявка одобрена (спросите администратора)

**Q: Случайно выбрал роль продавца, как вернуться?**  
A: Нажмите кнопку "🔙 Назад к выбору роли" в меню или отправьте `/start`

---

### 📦 Работа с товарами (для продавцов)

**Q: Как создать товар?**  
A: 
1. В меню продавца выберите "➕ Создать товар"
2. Клавиатура автоматически скроется для удобного ввода
3. Введите название товара (минимум 3 символа)
4. Введите описание (минимум 10 символов)
5. Укажите цену (число больше 0)
6. Укажите количество на складе
7. Выберите категорию из списка
8. Выберите локацию (страна → город → район)
9. Загрузите фотографию товара
10. Товар создан! Клавиатура автоматически вернётся

**Q: При создании товара случайно нажимаю кнопки вместо ввода текста**  
A: Это исправлено! Теперь при создании товара основная клавиатура скрывается и показывается только кнопка "❌ Отмена". После создания клавиатура автоматически возвращается.

**Q: Как отменить создание товара?**  
A: Нажмите кнопку "❌ Отмена" в любой момент создания товара. Клавиатура вернётся автоматически.

**Q: Как редактировать товар?**  
A:
1. Выберите "📦 Мои товары"
2. Нажмите на товар, который хотите изменить
3. Выберите "✏️ Редактировать товар"
4. Выберите поле для редактирования:
   - 📝 Название
   - 📄 Описание
   - 💰 Цена
   - 📊 Остаток на складе
   - 📁 Категория
   - 📍 Локация
   - 🖼️ Изображение
5. Введите новые данные
6. Изменения сохранятся автоматически

**Q: Как удалить товар?**  
A:
1. Выберите "📦 Мои товары"
2. Нажмите на товар
3. Выберите "🗑️ Удалить товар"
4. Подтвердите удаление
5. Товар будет удалён навсегда

**Q: Почему не могу загрузить фото товара?**  
A: Убедитесь, что:
- Отправляете именно фотографию (не файл)
- Размер фото не превышает 10 МБ
- Фото в формате JPG, PNG или WEBP

**Q: Товар создался с названием "➕ Создать товар"**  
A: Это была ошибка в старой версии, когда клавиатура не скрывалась при создании товара. Теперь это исправлено! Удалите старый товар и создайте новый.

---

### 🌍 Локации и категории

**Q: Не вижу нужную страну/город/район**  
A: Попросите администратора добавить нужную локацию через админ-панель:
- 🌍 Локации → Добавить страну/город/район

**Q: Как посмотреть доступные локации?**  
A (для продавца):
1. Выберите "🌍 Локации и категории"
2. Просмотрите список всех стран, городов и районов

**Q: Не вижу нужную категорию**  
A: Попросите администратора добавить категорию через:
- 📁 Категории → Добавить категорию

---

### 🛒 Покупки (для покупателей)

**Q: Как купить товар?**  
A:
1. Выберите "🏪 Магазины"
2. Выберите локацию (страна → город → район)
3. Выберите продавца
4. Просмотрите товары
5. Нажмите "🛒 В корзину" на понравившемся товаре
6. Перейдите в "🛒 Корзина"
7. Оформите заказ

**Q: Как добавить товар в избранное?**  
A: В карточке товара нажмите "⭐ В избранное". Все избранные товары доступны в разделе "⭐ Избранное".

**Q: Как найти конкретный товар?**  
A: Используйте раздел "🔍 Поиск":
- Введите название товара
- Примените фильтры по категории и цене (кнопка "🔍 Расширенный поиск")

---

### 👑 Администрирование

**Q: Как верифицировать продавца?**  
A:
1. Откройте админ-панель
2. Выберите "✅ Верификация"
3. Просмотрите список заявок
4. Одобрите или отклоните заявку
5. Продавец получит уведомление автоматически

**Q: Не приходят уведомления о новых заявках**  
A: Проверьте:
1. Бот добавлен в канал уведомлений как администратор
2. NOTIFICATION_CHANNEL_ID правильно указан в config.py
3. Канал не заблокирован и доступен

**Q: Уведомления приходят только в канал, но не администратору**  
A: Это нормально! Система работает так:
- Уведомление отправляется И в канал И всем администраторам одновременно
- Если один не доходит, другой всё равно придёт
- Проверьте, что ваш ID есть в SUPER_ADMIN_IDS

**Q: Как добавить новую страну/город/район?**  
A:
1. Админ-панель → "🌍 Локации"
2. Выберите нужное действие:
   - 🌍 Добавить страну
   - 🏙️ Добавить город (сначала выберите страну)
   - 📍 Добавить район (сначала выберите город)
3. Введите название
4. Локация добавлена!

**Q: Как изменить баланс пользователя?**  
A:
1. Админ-панель → "👥 Пользователи"
2. Выберите "💰 Управление балансом"
3. Введите ID пользователя
4. Выберите действие (пополнить/списать)
5. Укажите сумму

**Q: Как заблокировать пользователя?**  
A:
1. Админ-панель → "👥 Пользователи"
2. Выберите "🚫 Блокировка пользователей"
3. Введите ID пользователя
4. Подтвердите блокировку
5. Пользователь получит уведомление

**Q: Как просмотреть/модерировать товары?**  
A:
1. Админ-панель → "📦 Товары" (если доступно)
2. Или используйте поиск по товарам
3. Можете активировать/деактивировать/удалить товары

---

### 🐛 Решение проблем

**Q: Бот не отвечает на команды**  
A: Проверьте:
1. Бот запущен: `ps aux | grep python | grep main.py`
2. Проверьте логи: `tail -50 shop_q.log`
3. Перезапустите бота:
   - systemd: `sudo systemctl restart shop-q`
   - Docker: `docker-compose restart`
   - Вручную: остановите (Ctrl+C) и запустите `python main.py`

**Q: Ошибка "Foreign key constraint failed"**  
A: Это значит, что вы пытаетесь добавить данные с несуществующей связью:
- При добавлении города: убедитесь, что страна существует
- При добавлении района: убедитесь, что город существует
- При создании товара: убедитесь, что выбранная локация существует

**Q: Ошибка "UNIQUE constraint failed"**  
A: Объект с таким названием уже существует:
- Для стран: используйте уникальное название
- Для городов: название должно быть уникальным в пределах страны
- Для категорий: измените название

**Q: Слишком большой файл логов**  
A: Очистите логи:
```bash
# Просмотр размера
ls -lh shop_q.log

# Очистка
> shop_q.log

# Или настройте ротацию логов (см. раздел "Обслуживание")
```

**Q: База данных повреждена**  
A: Восстановите из резервной копии:
```bash
# Проверка целостности
sqlite3 shop_q.db "PRAGMA integrity_check;"

# Восстановление
cp shop_q_backup.db shop_q.db

# Перезапуск бота
sudo systemctl restart shop-q
```

**Q: Бот потребляет много памяти**  
A:
1. Перезапустите бота
2. Очистите старые данные из БД (см. раздел "Обслуживание")
3. Оптимизируйте базу: `sqlite3 shop_q.db "VACUUM;"`

---

### 📱 Использование

**Q: Как получить реферальную ссылку?**  
A: 
1. Откройте "👤 Профиль"
2. Найдите свой реферальный код
3. Поделитесь ссылкой: `https://t.me/AldanLiverisBot?start=ВАШ_КОД`

**Q: Сколько получу за реферала?**  
A: Настройки в config.py:
- Приглашающий: 100₽
- Приглашённый: 50₽

**Q: Как посмотреть свою статистику продаж?**  
A (для продавца):
1. Выберите "📊 Статистика"
2. Просмотрите:
   - Количество товаров
   - Количество продаж
   - Общую выручку
   - Средний рейтинг

**Q: Где посмотреть историю заказов?**  
A (для покупателя):
- Выберите "📦 Мои заказы"
- Для продавца: "🧾 Продажи"

---

### 🔐 Безопасность

**Q: Кто-то получил доступ к боту, что делать?**  
A:
1. Немедленно смените токен бота через @BotFather
2. Обновите BOT_TOKEN в переменных окружения
3. Перезапустите бота
4. Проверьте логи на подозрительную активность

**Q: Как защитить базу данных?**  
A:
```bash
# Ограничить права доступа
chmod 600 shop_q.db

# Делать регулярные бэкапы
cp shop_q.db shop_q_backup_$(date +%Y%m%d).db

# Хранить бэкапы в безопасном месте
```

**Q: Можно ли использовать бота для production?**  
A: Текущая версия подходит для малых/средних проектов. Для крупных проектов рекомендуется:
- PostgreSQL вместо SQLite
- Webhook вместо polling
- Redis для кэширования
- Мониторинг (Sentry, Prometheus)
- Load balancing

---

### 📞 Дополнительная помощь

**Не нашли ответ на свой вопрос?**

1. Проверьте логи: `tail -f shop_q.log`
2. Изучите документацию: README.md и TZ-SHOP.md
3. Обратитесь к администратору бота
4. Создайте issue в репозитории проекта

**Полезные команды для диагностики:**
```bash
# Статус бота
ps aux | grep python | grep main.py

# Последние логи
tail -50 shop_q.log

# Ошибки в логах
grep ERROR shop_q.log | tail -20

# Размер базы данных
ls -lh shop_q.db

# Проверка целостности БД
sqlite3 shop_q.db "PRAGMA integrity_check;"
```

---

## 📄 Лицензия

Этот проект создан для демонстрационных и образовательных целей.

---

## 👨‍💻 Автор

**SHOP-Q Team**

Дата создания: 2025
Версия: 1.0.0

---

**Разработано с ❤️ для SHOP-Q Marketplace**
